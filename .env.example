# PostgreSQL (docker compose)
POSTGRES_USER=minicloud
POSTGRES_PASSWORD=minicloud
POSTGRES_DB=minicloud
POSTGRES_PORT=5432

# Go API — used when running the API on the host (not in compose)
DATABASE_URL=postgres://minicloud:minicloud@localhost:5432/minicloud?sslmode=disable
PORT=8080
